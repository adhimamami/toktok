name: RDP with Ngrok - Complete & Alternative ISO

on:
  push:
  workflow_dispatch:

jobs:
  setup_rdp:
    # Anda bisa mengubah runner image di sini: windows-latest, windows-2022, atau windows-2019
    runs-on: windows-2022

    steps:
      # 1️⃣ Download Ngrok
      - name: Download Ngrok
        run: |
          Invoke-WebRequest -Uri "https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-windows-amd64.zip" -OutFile "ngrok.zip"
          Write-Host "✅ Ngrok ZIP downloaded!"
        shell: pwsh

      # 2️⃣ Ekstrak Ngrok
      - name: Extract Ngrok
        run: |
          Expand-Archive -Path "ngrok.zip" -DestinationPath "ngrok"
          Write-Host "✅ Ngrok extracted!"
        shell: pwsh

      # 3️⃣ Verifikasi Ngrok Executable
      - name: Check Ngrok Executable
        run: |
          if (-Not (Test-Path "ngrok\ngrok.exe")) {
            Write-Host "❌ ERROR: Ngrok executable not found!"
            exit 1
          } else {
            Write-Host "✅ Ngrok executable found!"
          }
        shell: pwsh

      # 4️⃣ Set Ngrok Auth Token
      - name: Set Ngrok Auth Token
        env:
          NGROK_AUTH_TOKEN: ${{ secrets.NGROK_AUTH_TOKEN }}
        run: |
          .\ngrok\ngrok.exe config add-authtoken $Env:NGROK_AUTH_TOKEN
          Write-Host "✅ Ngrok authentication successful!"
        shell: pwsh

      # 5️⃣ Aktifkan RDP, Nonaktifkan NLA, dan Restart Layanan RDP
      - name: Enable RDP, Disable NLA, and Restart RDP Service
        run: |
          Write-Host "🔧 Configuring RDP..."
          # Aktifkan koneksi RDP
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          Write-Host "✅ RDP enabled!"

          # Nonaktifkan Network Level Authentication (NLA) untuk testing
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0
          Write-Host "✅ NLA disabled (UserAuthentication set to 0)"

          # Restart service RDP (TermService)
          Restart-Service -Name TermService -Force
          Write-Host "✅ TermService restarted!"
        shell: pwsh

      # 6️⃣ Buat User Baru dan Atur Password
      - name: Create User and Set Password
        run: |
          $username = "RDPUser"
          $password = ConvertTo-SecureString -AsPlainText "P@ssw0rd123!" -Force
          
          if (Get-LocalUser -Name $username -ErrorAction SilentlyContinue) {
              Write-Host "User '$username' already exists. Updating password..."
              Set-LocalUser -Name $username -Password $password
          } else {
              New-LocalUser -Name $username -Password $password -Description "RDP Access User" -PasswordNeverExpires
              Write-Host "User '$username' created."
          }
          
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member $username
          Write-Host "✅ User created/updated and added to Remote Desktop Users group!"
          Write-Host "🧑‍💻 Username: $username"
          Write-Host "🔑 Password: P@ssw0rd123!"
          
          # Debug: Tampilkan daftar user lokal dan anggota grup
          Write-Host "Daftar user lokal:"
          Get-LocalUser
          Write-Host "Anggota group 'Remote Desktop Users':"
          Get-LocalGroupMember -Group "Remote Desktop Users"
        shell: pwsh

      # 7️⃣ Jalankan Ngrok Tunnel dan Debug Port 3389
      - name: Start Ngrok Tunnel and Debug Port 3389
        run: |
          Write-Host "🚀 Starting Ngrok tunnel..."
          $ngrokProcess = Start-Process -FilePath ".\ngrok\ngrok.exe" -ArgumentList "tcp", "3389" -NoNewWindow -PassThru
          Start-Sleep -Seconds 20

          $ngrok_url = $null
          $attempts = 0
          while (-not $ngrok_url -and $attempts -lt 12) {
              try {
                  $response = Invoke-WebRequest -Uri http://127.0.0.1:4040/api/tunnels -UseBasicParsing -TimeoutSec 5
                  $tunnels = ($response.Content | ConvertFrom-Json).tunnels
                  foreach ($tunnel in $tunnels) {
                      if ($tunnel.public_url -like "tcp://*") {
                          $ngrok_url = $tunnel.public_url
                          break
                      }
                  }
              }
              catch {
                  Write-Host "Waiting for Ngrok tunnel..."
              }
              if (-not $ngrok_url) {
                  Start-Sleep -Seconds 5
                  $attempts++
              }
          }
            
          if (-not $ngrok_url) {
              Write-Host "❌ ERROR: Failed to retrieve Ngrok URL after multiple attempts."
              Stop-Process -Id $ngrokProcess.Id
              exit 1
          }

          Write-Host "✅ Ngrok Tunnel Ready!"
          Write-Host "🌍 Server Address: $ngrok_url"

          # Debug: Cek apakah port 3389 sudah mendengarkan
          Write-Host "Status port 3389:"
          netstat -an | findstr "3389"
        shell: pwsh

      # 8️⃣ Keep Alive agar Workflow tidak berhenti
      - name: Keep Alive
        run: |
          Write-Host "🟢 Keeping session alive..."
          while ($true) { Start-Sleep -Seconds 3600 }
        shell: pwsh

# Alternatif:
# Jika Anda ingin mencoba runner dengan image Windows yang berbeda (misalnya, windows-2019),
# Anda dapat menambahkan job baru atau mengubah baris 'runs-on' di atas.
