name: RDP with Ngrok

on: 
  - push
  - workflow_dispatch

jobs:
  setup_rdp:
    runs-on: windows-latest

    steps:
      # 1️⃣ Download Ngrok
      - name: Download Ngrok
        run: |
          Invoke-WebRequest -Uri "https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-windows-amd64.zip" -OutFile "ngrok.zip"
          Write-Host "✅ Ngrok ZIP downloaded!"
        shell: pwsh

      # 2️⃣ Ekstrak Ngrok
      - name: Extract Ngrok
        run: |
          Expand-Archive -Path "ngrok.zip" -DestinationPath "ngrok"
          Write-Host "✅ Ngrok extracted!"
        shell: pwsh

      # 3️⃣ Pastikan Ngrok Terekstrak dengan Benar
      - name: Check Ngrok Executable
        run: |
          if (-Not (Test-Path "ngrok\ngrok.exe")) {
            Write-Host "❌ ERROR: Ngrok executable not found!"
            exit 1
          } else {
            Write-Host "✅ Ngrok executable found!"
          }
        shell: pwsh

      # 4️⃣ Set Ngrok Auth Token
      - name: Set Ngrok Auth Token
        env:
          NGROK_AUTH_TOKEN: ${{ secrets.NGROK_AUTH_TOKEN }}
        run: |
          .\ngrok\ngrok.exe authtoken $Env:NGROK_AUTH_TOKEN
          Write-Host "✅ Ngrok authentication successful!"
        shell: pwsh

- name: Enable Remote Desktop
  run: |
    Write-Host "🔧 Configuring RDP..."
    
    # Izinkan koneksi RDP
    Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0

    # Pastikan firewall tidak memblokir RDP
    Enable-NetFirewallRule -DisplayGroup "Remote Desktop"

    # Atur metode otentikasi agar lebih kompatibel
    Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0

    # Set policy agar user bisa login dengan RDP
    New-ItemProperty -Path "HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Terminal Services" -Name "fDenyTSConnections" -PropertyType DWord -Value 0 -Force

    # Restart layanan Remote Desktop
    Restart-Service -Name TermService -Force
    Write-Host "✅ RDP configuration updated & service restarted!"
  shell: pwsh


      # 6️⃣ Atur Password User "runneradmin"
      - name: Set Admin Password
        run: |
          $password = ConvertTo-SecureString -AsPlainText "P@ssw0rd!" -Force
          Set-LocalUser -Name "runneradmin" -Password $password
          Write-Host "✅ Admin password set!"
        shell: pwsh

      # 7️⃣ Jalankan Ngrok di Background & Ambil URL
      - name: Start Ngrok Tunnel
        run: |
          Write-Host "🚀 Starting Ngrok tunnel..."
          Start-Process -FilePath ".\ngrok\ngrok.exe" -ArgumentList "tcp 3389" -NoNewWindow
          Start-Sleep -Seconds 10  # Beri waktu untuk Ngrok berjalan

          # Ambil URL Ngrok
          $ngrok_url = (Invoke-WebRequest -Uri http://127.0.0.1:4040/api/tunnels | ConvertFrom-Json).tunnels[0].public_url
          Write-Host "✅ Ngrok Tunnel Ready!"
          Write-Host "🌍 Server Address: $ngrok_url"
          Write-Host "🧑‍💻 Username: runneradmin"
          Write-Host "🔑 Password: P@ssw0rd!"
        shell: pwsh

      # 8️⃣ Keep Alive (Agar Workflow Tidak Berhenti)
      - name: Keep Alive
        run: |
          Write-Host "🟢 Keeping session alive..."
          while ($true) { Start-Sleep -Seconds 3600 }
        shell: pwsh
